# 🎹 Музыкальная машинка пидорасов 

> «Те, кто долго жил среди пидарасов, говорят, что они втайне стыдятся своего греха и стараются поразить всякими фокусами. Думают про себя так: «Да, я пидарас. Так уж вышло — что теперь делать… Но может быть, я гениальный пидарас! Вдруг я напишу удивительную музыку! Разве посмеют плохо говорить о гениальном музыканте…» И поэтому все время стараются придумать новую музыку, чтобы не стыдно было и дальше харить друг друга в дупло. И если б делали тихо, в специальном обитом пробкой месте, то всем было бы так же безразлично, как и то, что долбятся в сраку. Но их музыку приходится слушать каждый день, ибо заводят ее повсеместно. И потому не слышим ни ветра, ни моря, ни шороха листьев, ни пения птиц. А только один и тот же пустой и мертвый звук, которым хотят удивить, запуская его в небо под разными углами.
> Бывает, правда, что у пидарасов ломается музыкальная установка. В такие минуты спеши слушать тишину.»
>
> — **Виктор Пелевин, S.N.U.F.F.**

## 📢 Манифест
Пока разнообразные «продюсеры», завороженно греясь у видеокарт, хвастаются своими «муравьиными фермами» и пытаются удержать монополию на производство своей нормисной параши, мы отдаем этот конвейер в руки каждого. 

## 🛠 Технический стек
Проект представляет собой оркестратор распределенных LLM-провайдеров и локального аудио-движка. Оптимизирован под **Apple Silicon (M4)**, но легко адаптируется под **NVIDIA/CUDA**.

*   **Тексты (Lyrics Manager):** 
    *   Библиотека **`g4f`** тянет смыслы из 50+ провайдеров (OpenAI, DeepSeek, Anthropic).
    *   **Обход цензуры:** Если нейронка выдает морализаторство вместо трэша, она летит в бан, а запрос уходит следующей.
    *   **Принудительная деградация:** Воркер жестко вклеивает рефрен про «письки-сиськи» в каждый текст, гарантируя стабильное качество продукта.
*   **Звук (Audio Engine):**
    *   Локальный сервер **ACE Step 1.5**. 
    *   Генерация идет отрезками по 90 секунд (сегментами).
*   **Сборка (Audio Utils):**
    *   **SoX**: Эффект `bend` (pitch rise) на стыке сегментов для складного нонстопа (тональности при генерации тоже переключаются каруселькой).
    *   **FFmpeg**: Бесшовный `acrossfade` (15 сек) для создания бесконечного потока говна.

## ⚙️ Механика конвейера (подробно)
Система работает как замкнутый цикл производства:

1.  **Сырье (Text Worker):** Асинхронный воркер в фоне забивает очередь текстами. Он берет случайные строчки из случайных песее из базы `collected_lyrics.txt` (сгенерированный моделью обученной на топ200 спотифай + немного реальных текстов), скармливает массив LLM с требованием сделать из этого «дорогой трэш» и ждет ответа. Очередь держит запас в 15 текстов, чтобы аудио-движок не простаивал. Так же генерируется описание музыки. Как только будет получена первая пара промптов музыка+текст, запускается производство песни. Тем временем воркер долбится за следующими текстами.
2.  **Синтез (ACE Engine):** Программа берет 2 промпта из очереди и отправляет его на локальный API ACE Step. Генерируется `.flac` файл. Каждый следующий трек повышает тональность на полутон относительно предыдущего. Если сервер ACE падает (что часто бывает при утечках памяти), `ace_engine` его убивает и поднимает заново, не прерывая общий цикл.
3.  **Обработка стыков:** Чтобы переход между 90-секундными кусками двух разных тональностей не звучал как интервал дьявола, каждый трек обрабатывается через `SoX`. Применяется эффект `bend`, плавно завышающий тональность на полтона к концу. Так на стыках песен тональности полностью совпадают.
4.  **Сшивка альбома:** Когда сегментов набирается на 60 минут (`TARGET_TOTAL_SECONDS`), вызывается FFmpeg. Он накладывает куски друг на друга с 15-секундным нахлестом (`acrossfade`), создавая часовой микс.
5.  **Доставка:** Готовый файл летит в Telegram-канал через `telethon` (надо указать id канала в config.py).

## 📁 Устройство системы
| Файл | Назначение |
| :--- | :--- |
| `main.py` | Диспетчер. Координирует воркер текстов и цикл сборки альбомов. |
| `lyrics_manager.py` | Эксплуатация внешних LLM, чистка ответов от «мыслей» нейронки. |
| `ace_engine.py` | Прослойка для общения с ACE Step, контроль «живучести» сервера. |
| `ace_wrapper.py` | Запуск ACE Step в фоне с подавлением мусорных логов (tqdm). |
| `audio_utils.py` | Хирургия звука: Pitch Rise через SoX и склейка через FFmpeg. |
| `config.py` | Параметры: BPM, тональности, тайминги кроссфейда. |

## 🧪 Адаптация под CUDA (NVIDIA)
Если вы запускаете это не на Mac, а на нормальной видеокарте с CUDA:

1.  **Измените девайс в `ace_wrapper.py`:**
    ```python
    # Найдите строку запуска:
    cmd += ["--device", "cpu"] # Замените "cpu" на "cuda"
    ```
2.  **Измените инициализацию в `ace_engine.py`:**
    В функции `initialize_ace_model` замените аргумент `"cpu"` на `"cuda"` в вызове `client.predict`.
3.  **Убедитесь в наличии PyTorch с поддержкой CUDA:**
    ```bash
    pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
    ```

## 🖥 Системные требования
Совпадают с требованиями ACE Step 1.5.

*   **Софт:**
    *   `ffmpeg` (с поддержкой `libmp3lame` или `aac`).
    *   `sox` (с поддержкой `flac`).
    *   Python 3.11.
 
 ## Бенчмарк
 Apple Silicon M4 выдает часовой альбом примерно за 41 минуту на длине трека 90 секунд в режиме CPU. Ходят слухи, что CUDA на GPU будет делать это быстрее    

## 🚀 Инструкция по эксплуатации

1.  **Подготовка:**
    *   Установите локально **ACE Step 1.5** (склонируйте их репо и сделайте `pip install -e .`).
    *   Установите системные утилиты: `brew install ffmpeg sox` (macOS) или `apt install ffmpeg sox libsox-fmt-all` (Linux).
2.  **Зависимости:**
    ```bash
    pip install g4f gradio_client telethon requests
    ```
3.  **Конфиг:**
    В файле `config.py` пропишите свои `API_ID` и `API_HASH` и 'channelid' для Telegram.
4.  **Запуск:**
    ```bash
    python main.py
    ```

## ⚖️ Лицензия
**WTFPL** — делайте что хотите.
