# 🎹 Музыкальная машинка пидорасов 

> «Те, кто долго жил среди пидарасов, говорят, что они втайне стыдятся своего греха и стараются поразить всякими фокусами. Думают про себя так: «Да, я пидарас. Так уж вышло — что теперь делать… Но может быть, я гениальный пидарас! Вдруг я напишу удивительную музыку! Разве посмеют плохо говорить о гениальном музыканте…» И поэтому все время стараются придумать новую музыку, чтобы не стыдно было и дальше харить друг друга в дупло. И если б делали тихо, в специальном обитом пробкой месте, то всем было бы так же безразлично, как и то, что долбятся в сраку. Но их музыку приходится слушать каждый день, ибо заводят ее повсеместно. И потому не слышим ни ветра, ни моря, ни шороха листьев, ни пения птиц. А только один и тот же пустой и мертвый звук, которым хотят удивить, запуская его в небо под разными углами.
> Бывает, правда, что у пидарасов ломается музыкальная установка. В такие минуты спеши слушать тишину.»
>
> — **Виктор Пелевин, S.N.U.F.F.**

## 📢 Манифест
Пока разнообразные «продюсеры», завороженно греясь у видеокарт, хвастаются своими «муравьиными фермами» и пытаются удержать монополию на производство своей нормисной параши, мы отдаем этот конвейер в руки каждого. 


## 🛠 Технический стек
Проект оптимизирован под **Apple Silicon (M4)** и представляет собой оркестратор распределенных LLM-провайдеров и локального аудио-движка.

*   **Тексты (Lyrics Manager):** 
    *   Использует библиотеку **`g4f`** для доступа к 50+ внешним LLM-провайдерам (OpenAI, Google, DeepSeek, Bing).
    *   **Обход цензуры:** Автоматическая ротация провайдеров. Если нейронка отказывается писать трэш, она летит в бан на 10 минут, а запрос уходит следующей.
    *   **Принудительная деградация:** В каждый сгенерированный текст воркер принудительно вшивает рефрен про «письки-сиськи», уничтожая любые зачатки «интеллектуальности».
*   **Звук (Audio Engine):**
    *   Локальный сервер **ACE Step 1.5**. 
    *   Генерация треков по 90 секунд, которые затем сшиваются в часовые альбомы.
*   **Сборка (Audio Utils):**
    *   **SoX**: Наложение эффекта `bend` (нарастающий pitch rise) на стыках сегментов.
    *   **FFmpeg**: Склейка через `acrossfade` для создания бесшовного «микса».

## 📁 Устройство системы
| Файл | Назначение |
| :--- | :--- |
| `main.py` | Главный диспетчер. Запускает `text_provider_worker` и цикл сборки альбомов. |
| `lyrics_manager.py` | Ядро эксплуатации внешних нейронок. |
| `ace_engine.py` | Управление локальным ACE сервером через Gradio Client. |
| `ace_wrapper.py` | Скрипт-невидимка для запуска ACE Step без графического мусора. |
| `audio_utils.py` | Грязная работа со звуком: SoX bend и FFmpeg concat. |
| `config.py` | Настройки: таргет — альбомы по 60 минут, 325 штук за цикл. |

## 🚀 Инструкция по эксплуатации

1.  **Подготовка:**
    *   Установите локально **ACE Step 1.5**.
    *   Установите системные утилиты: `brew install ffmpeg sox`.
2.  **Зависимости:**
    ```bash
    pip install g4f gradio_client telethon requests
    ```
3.  **Конфиг:**
    В файле `config.py` пропишите свои `API_ID` и `API_HASH` для Telegram, чтобы говномат мог выплескивать результат в канал.
4.  **Запуск:**
    ```bash
    python main.py
    ```

## ⚖️ Лицензия
**WTFPL** — делайте что хотите. 
